export default{
    login(context, user = {}){
        // let userObj = {
        //     userName:user.name,
        //     userId:user.id, 
        //     userPwd:user.pwd
        // }
        // context.commit('login',userObj)
    }
}